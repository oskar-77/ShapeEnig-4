# دليل مشروع Mr.OSKAR (Template Engine) 🚀

هذا المشروع عبارة عن محرك قوالب تفاعلي متقدم يعتمد على الجسيمات (Particles) والنمذجة ثلاثية الأبعاد باستخدام تقنيات الويب الحديثة.

## 🛠 الأدوات والتقنيات المستخدمة

### 1. الواجهة الأمامية (Frontend)
- **React.js**: الإطار الأساسي لبناء واجهة المستخدم.
- **Three.js**: المكتبة المستخدمة في معالجة الرسوميات ثلاثية الأبعاد ومحاكاة الجسيمات.
- **Tailwind CSS**: لتنسيق الواجهة بتصميم عصري (Glassmorphism).
- **Framer Motion**: لإضافة حركات انتقالية سلسة وتفاعلية.
- **Lucide React**: لمجموعة الأيقونات المستخدمة في الواجهة.
- **TanStack Query (React Query)**: لإدارة حالات جلب البيانات والمزامنة مع الخادم.

### 2. الواجهة الخلفية (Backend)
- **Node.js & Express**: لبناء خادم الويب والـ API.
- **Drizzle ORM**: للتعامل مع قاعدة البيانات بطريقة Type-safe.
- **PostgreSQL**: قاعدة البيانات المستخدمة لتخزين القوالب وأوصاف الأشكال.
- **Zod**: للتحقق من صحة البيانات (Data Validation).

## 🏗 بنية المشروع

- `/client`: يحتوي على كود الواجهة الأمامية (React components, hooks, pages).
- `/server`: يحتوي على منطق الخادم، المسارات (Routes)، وإعدادات قاعدة البيانات.
- `/shared`: يحتوي على المخططات (Schemas) المشتركة والأنواع (Types).
- `/attached_assets`: مخصص لتخزين الصور والملفات المرفقة.

## 🚀 المعالجة والعرض

1. **محاكي الجسيمات (Particle Viewer)**: يقوم النظام بتحويل الأشكال الهندسية أو الصور المرفوعة إلى آلاف الجسيمات النقطية التي تتفاعل مع حركة الماوس (التنافر والجاذبية).
2. **إدارة القوالب**: يمكن للمستخدم اختيار قوالب جاهزة (مثل Sphere, Helix, Torus) أو رفع صور خاصة لتحويلها إلى تشكيلات جسيمات.
3. **التوثيق الديناميكي**: يدعم النظام عرض تفاصيل كل شكل باستخدام Markdown، مما يسمح بشرح الخصائص الرياضية أو الفيزيائية لكل نموذج.

## 💻 كيفية التطوير والتشغيل

- **بيئة التطوير**: يتم تشغيل المشروع باستخدام `npm run dev` الذي يشغل Vite للواجهة الأمامية و tsx للخادم.
- **قاعدة البيانات**: يتم تحديث المخطط باستخدام `npm run db:push`.
- **البناء للإنتاج**: يتم استخدام `npm run build` لتجهيز الملفات النهائية.

---
تم إعداد هذا الدليل لمساعدتك في فهم وتطوير مشروع **Mr.OSKAR**.